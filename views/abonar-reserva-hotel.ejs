<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Hacer abono de reserva</title>
    <style>
        body {
            background: linear-gradient(120deg, #fffbe6 0%, #ffe0b2 100%);
            font-family: 'Montserrat', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .abono-container {
            max-width: 440px;
            margin: 3.5rem auto;
            background: #fffdfa;
            border-radius: 22px;
            box-shadow: 0 6px 32px rgba(255,179,0,0.18), 0 1.5px 8px rgba(0,0,0,0.07);
            padding: 2.5rem 2.2rem 2rem 2.2rem;
            text-align: center;
            border: 2.5px solid #ffb300;
            position: relative;
        }
        .abono-container h1 {
            color: #ff9800;
            font-size: 1.5rem;
            margin-bottom: 1.7rem;
            font-weight: 800;
            letter-spacing: 0.5px;
            text-shadow: 0 2px 8px #ffe0b2;
        }
        .abono-info {
            background: #fffbe6;
            border-radius: 10px;
            padding: 1.2rem 1rem;
            margin-bottom: 1.5rem;
            color: #444;
            font-size: 1.08rem;
            text-align: left;
        }
        .btn-abonar {
            display: inline-block;
            background: linear-gradient(90deg, #ffb300 60%, #ff9800 100%);
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 0.85rem 2.2rem;
            font-size: 1.15rem;
            font-weight: 800;
            margin-top: 1.3rem;
            margin-bottom: 0.5rem;
            cursor: pointer;
            box-shadow: 0 2px 12px rgba(255,179,0,0.13);
            letter-spacing: 0.5px;
            transition: background 0.2s, transform 0.2s;
        }
        .btn-abonar:hover {
            background: linear-gradient(90deg, #ff9800 60%, #ffb300 100%);
            transform: translateY(-2px) scale(1.04);
        }
        .btn-volver {
            background: #bbb;
            color: #222;
            border-radius: 8px;
            padding: 0.6rem 1.5rem;
            font-size: 1rem;
            font-weight: 600;
            margin-left: 0.5rem;
            margin-top: 1.3rem;
            text-decoration: none;
            transition: background 0.2s, color 0.2s;
        }
        .btn-volver:hover {
            background: #888;
            color: #fff;
        }
        .icono-abono {
            font-size: 2.5rem;
            color: #ffb300;
            margin-bottom: 0.7rem;
            text-shadow: 0 2px 8px #ffe0b2;
        }
    </style>
</head>
<body>
    <div class="abono-container">
        <div class="icono-abono"><i class="fas fa-wallet"></i></div>
        <h1>Hacer abono de reserva</h1>
        <div class="abono-info">
            <div><strong>Hotel:</strong> <%= reserva.hotel_nombre %></div>
            <div><strong>Habitación:</strong> <%= reserva.habitacion_tipo %></div>
            <div><strong>Check-in:</strong> <%= reserva.fecha_entrada.toISOString().split('T')[0] %></div>
            <div><strong>Check-out:</strong> <%= reserva.fecha_salida.toISOString().split('T')[0] %></div>
            <div><strong>Huéspedes:</strong> <%= reserva.numero_huespedes %></div>
            <div><strong>Precio total:</strong> $<%= reserva.precio_total %></div>
            <div style="color:#ff9800;"><strong>Valor del abono:</strong> $<%= (reserva.precio_total / 2).toFixed(2) %></div>
        </div>
        <form action="/hotel/reserva/abonar/<%= reserva.id %>" method="POST">
            <button type="submit" class="btn-abonar">Confirmar abono</button>
            <a href="/bookings" class="btn-volver">Volver</a>
        </form>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/js/all.min.js"></script>
</body>
</html> 