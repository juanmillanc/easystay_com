<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EasyStay - Mi perfil</title>
    <link rel="stylesheet" href="/css/search.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* Estilo para el botón de volver */
        .back-to-home {
            position: absolute;
            top: 20px;
            left: 20px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background-color: #007faf;
            color: white;
            border-radius: 50%;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            z-index: 10;
        }
        
        .back-to-home:hover {
            background-color: #006090;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.3);
        }
        
        .back-to-home i {
            font-size: 1.2rem;
        }
        
        /* Ajuste para móviles */
        @media (max-width: 768px) {
            .back-to-home {
                top: 15px;
                left: 15px;
                width: 36px;
                height: 36px;
            }
        }
        
        /* Ajuste para el main */
        .profile-main {
            position: relative;
            padding-top: 20px;
        }
        
        .profile-section {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            margin: 30px auto;
            max-width: 500px;
            padding: 32px 24px;
        }
        
        .profile-details {
            margin-top: 20px;
        }
        
        .profile-info-card {
            background: #f7f7f7;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
        }
        
        .profile-detail {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        
        .profile-detail strong {
            color: #007faf;
        }
        
        .profile-actions {
            display: flex;
            gap: 16px;
            margin-top: 20px;
        }
        
        .action-button {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 10px 18px;
            border-radius: 20px;
            text-decoration: none;
            font-weight: 600;
            transition: background 0.2s;
        }
        
        .primary-button {
            background: #007faf;
            color: #fff;
        }
        
        .primary-button:hover {
            background: #005f7a;
        }
        
        .secondary-button {
            background: #e0e0e0;
            color: #333;
        }
        
        .secondary-button:hover {
            background: #bdbdbd;
        }
        
        .error {
            color: #d32f2f;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <%- include('partials/footer') %>
    <main class="profile-main">
        <a href="/" class="back-to-home">
            <i class="fas fa-arrow-left"></i> 
        </a>
        <section class="profile-section">
            <center><h1>Mi perfil</h1></center>
            
            <% if (typeof error !== 'undefined' && error) { %>
                <p class="error"><%= error %></p>
            <% } %>
            
            <div class="profile-details">
                <div class="profile-info-card">
                    <h2>Información básica</h2>
                    
                    <div class="profile-detail">
                        <strong>ID de usuario</strong>
                        <span><%= user.id %></span>
                    </div>
                    
                    <div class="profile-detail">
                        <strong>Nombre completo</strong>
                        <span><%= user.nombre %></span>
                    </div>
                    
                    <div class="profile-detail">
                        <strong>Correo electrónico</strong>
                        <span><%= user.email %></span>
                    </div>
                    
                    <div class="profile-detail">
                        <strong>Teléfono</strong>
                        <span><%= user.telefono || 'No especificado' %></span>
                    </div>
                </div>
                
            </div>
            
            <div class="profile-actions">
                <a href="/editar-perfil" class="action-button primary-button">
                    <i class="fas fa-user-edit"></i> Editar perfil
                </a>
                <a href="/editar-perfil#cambiar-contrasena" class="action-button secondary-button">
                    <i class="fas fa-lock"></i> Cambiar contraseña
                </a>
            </div>
        </section>
        
        <section class="profile-section">
            <h2>Actividad reciente</h2>
            <p>Aquí iría tu historial de reservas y actividad</p>
            <!-- Puedes añadir una lista de reservas recientes aquí -->
        </section>
    </main>
    <footer class="site-footer">
        <!-- Mantén tu footer existente -->
    </footer>
    <script src="/js/search.js"></script>
</body>
</html>